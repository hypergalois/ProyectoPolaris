const roles = {
    ADMIN: 'admin',
    CREATOR: 'creator',
    USER: 'user'
};

const status = {
    PENDING: 'pending',
    ACCEPTED: 'accepted',
    REJECTED: 'rejected'
};

export default {
    roles,
    status
};